{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --outdir=./dist --platform=node --external:@aws-sdk/*"
  },
  "dependencies": {
    "@aws-sdk/client-codebuild": "^3.321.1",
    "@aws-sdk/client-s3": "^3.245.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.95",
    "esbuild": "^0.14.39"
  }
}
